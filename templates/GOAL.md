<!-- GOAL.md — the single objective this Ralph loop drives to completion. -->
<!-- Replace everything below with your goal, then re-run `ralph <dir>`. -->
<!-- Be concrete and bound the scope. State explicit acceptance criteria -->
<!-- the VALIDATE phase can check by running a real command. -->
<!-- -->
<!-- Example: -->
<!-- Create add.py exposing add(a, b) and a passing pytest test_add.py. -->
<!-- Acceptance: `pytest -q` exits 0 with >=1 test for add(). Nothing else. -->
<!-- -->
<!-- (This file is all comments, so the loop refuses to start until you -->
<!--  put a real, non-comment line here.) -->
